# Alternative scrape config if you later decide to edit dLogs Prometheus config.
# dAgent itself does not apply this file.

- job_name: 'dagent-worker'
  metrics_path: /metrics
  static_configs:
    - targets:
        - 'host.docker.internal:8765'
      labels:
        source: host
        app: dagent
        worker: main

